//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

@isTest
public class RPM_StorageLocationControllerMockImpl implements WebServiceMock {
	public void doInvoke(
			Object stub,
			Object request,
			Map<String, Object> response,
			String endpoint,
			String soapAction,
			String requestName,
			String responseNS,
			String responseName,
			String responseType) {
       
		System.debug(LoggingLevel.INFO, 'RPM_StorageLocationControllerMockImpl.doInvoke() - ' +
			'\n request: ' + request +
			'\n response: ' + response +
			'\n endpoint: ' + endpoint +
			'\n soapAction: ' + soapAction +
			'\n requestName: ' + requestName +
			'\n responseNS: ' + responseNS +
			'\n responseName: ' + responseName +
			'\n responseType: ' + responseType);

		if(request instanceOf RPM_StorageLocationController.IAAI_STOR_LOC_DETAIL_element) {
			response.put( 'response_x', new RPM_StorageLocationController.IAAI_STOR_LOC_DETAILResponse_element());
		}
		else if(request instanceOf RPM_StorageLocationController.IAAI_STORAGE_LOCATION_LOOKUP_element) {
			response.put( 'response_x', new RPM_StorageLocationController.IAAI_STORAGE_LOCATION_LOOKUPResponse_element());
		}
		else if(request instanceOf RPM_StorageLocationController.IAAI_STORAGE_LOCATION_MAINTENANCE_element) {
			response.put( 'response_x', new RPM_StorageLocationController.IAAI_STORAGE_LOCATION_MAINTENANCEResponse_element());
		}
	}
}